name: Denopendabot
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"
jobs:
  update:
    name: Update dependencies
    runs-on: ubuntu-latest
    steps:
      - uses: hasundue/denopendabot@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
